<div class="container-fluid">
    <div class="row col-md-12">
        <div class="col-md-12 text-center" style="margin: 10px 10px 20px 10px">
            <a href="/minors_courses/new"><button class="btn btn-default">Add A Minor's Course</button></a>
        </div>

        <% @minors.each do |minor| %>
            <div class="panel panel-default col-md-4" style="padding: 0 0 0 0; margin: 10px">
                <div class="panel-heading">
                    <%= minor.name %>
                </div>

                <% minor.course.each do |course| %>
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <div class="col-sm-10">
                                <p><%= course.subject %> <%= course.number %> - <%= course.credit %></p>
                                <p><%= course.title %></p>
                            </div>
                            <%= link_to minors_course_path(:minor_id => minor.id, :id => course.id), :method => :delete, :data => {:confirm => 'Are you sure?'}, :class => "col-sm-2" do %>
                                <i class="fa fa-times"></i>
                            <% end %>
                        </div>
                    </div>
                <% end %>
            </div>
        <% end %>
                

        </div>
    </div>
</div>