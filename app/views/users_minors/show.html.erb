<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 text-center" style="margin: 10px 10px 20px 10px">
            <a href="/users_minors/new"><button class="btn btn-default">Add A Minor</button></a>
        </div>
        <div class="col-md-8 col-md-offset-2">
            <div class="panel panel-default">
                <div class="panel-heading"><%= @user.name %>'s Minors</div>
                <div class="panel-body">
                    <% @user.minor.each do |user_minor| %>
                        <div class="panel panel-default col-md-4">
                            <div class="panel-body">
                                <div class="col-sm-10">
                                    <p><%= user_minor.name %></p>
                                </div>
                                <%= link_to users_minor_path(:user_id => @user.id, :id => user_minor.id), :method => :delete, :data => {:confirm => 'Are you sure?'}, :class => "col-sm-2" do %>
                                    <i class="fa fa-times"></i>
                                <% end %>
                            </div>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>